import { ActivoInactivoPipe } from './activo-inactivo.pipe';

describe('ActivoInactivoPipe', () => {
  it('create an instance', () => {
    const pipe = new ActivoInactivoPipe();
    expect(pipe).toBeTruthy();
  });
});
